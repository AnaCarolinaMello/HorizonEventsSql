<div id="navbar">
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="#">
                <img src="../imgNative/logoBranca.jpg" width="30" height="30" class="d-inline-block align-top" alt="">
                Horizon of events
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="#">Home <span class="sr-only"></span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Eventos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/search">Pesquisar</a>
                    </li>
                </ul>
                <div id="logout">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a href="/logout"><button class="nav-link" id="btnLogout">Logout</button></a>
                        </li>
                    </ul>
                </div>
            </div>
</div>
</nav>
</header>
{{#if erros}}
<div class="alert alert-danger">
  <center>
    {{#each erros}}
    <label class="errosTexto">*{{texto}}</label>
    {{else}}

    {{/each}}
</div>
</center>
{{/if}}

<div id="flashMsg">
    {{#if success_msg}}
    <div class="alert alert-success">
      <p>{{success_msg}}</p>
    </div>
    {{/if}}

    {{#if error_msg}}
    <div class="alert alert-danger">
      <p>{{error_msg}}</p>
    </div>
    {{/if}}
    {{#if warning_msg}}
    <div class="alert alert-warning">
      <p>{{warning_msg}}</p>
    </div>
    {{/if}}
  </div>

<main>
    <div id="containerMain">
        <div id="container">
            <div id="foto">
                <img src={{foto_perfil}} id="imagem">
            </div>
        
            <div id="dados">
                <div id="nomeEmpresa">
                    <h1>{{nome}}</h2>
                </div>
                <div id="dadosSecundarios">
                    <div id="divEndereco">
                        <h3>Endereço</h3>
                        <p>Rua: {{rua}}</p>
                        <p>Numero: {{numero}}</p>
                    </div>
                    <div id="divContatos">
                        <h3>Contato</h3>
                        <p>{{email}}</p>
                        <p>{{telefone}}</p>
                    </div>
                </div>
            </div>
        <div id="divFavoritar">
            <form action="/business/ViewBusiness?business={{id}}" method="post"> 
            {{#if salvar}}
              <label for="btnSalvar" class="heartEmpty">
                <span class="material-symbols-outlined" class="heartEmpty">favorite</span>
              </label>
            {{/if}}
            {{#if salvo}}
              <label for="btnSalvar" class="heartEmpty">
                <span class="material-symbols-outline" class="heartEmpty">favorite</span>
              </label>
            {{/if}}
            <button name={{id}} id="btnSalvar">aaa</button>
          </form>
        </div>
    </div>
</main>